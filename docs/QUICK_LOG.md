# Quick 小改记录

> 由 `/wxc-mini:quick` 维护。记录**不值得单开 FEAT**的小改动（bugfix / 文案 / 微调 / 小重构），每条必须留痕并同步相关文档。
>
> 判定：能一次做完、不引入新模块、不改稳定对外接口、不需要"讨论→spec"的，走 quick；否则走 `/wxc-mini:start`。

| # | 日期 | 摘要 | 涉及模块 | 涉及术语 | 改动文件 |
|---|---|---|---|---|---|
| 1 | 2026-09-07 | 新增【自定义】模式：编辑器画地图（矩形填/擦）+ 配雷数与 9 种道具数量，开局；打完可重开/返回编辑/回标题；JSON 导出（询问关卡名作文件名，地图为逐行 0/1 数组）与导入（Web 走 JavaScriptBridge，桌面走原生文件框）。自定义局不写存档。 | CustomLevel（新）, GameFlow | 自定义关卡（新） | `scripts/game/custom_level.gd`（新）、`scripts/ui/custom_level_editor.gd`（新）、`scripts/main.gd`、`scripts/ui/start_screen.gd`、`scripts/ui/regular_skip_control.gd`、`tests/test_custom_level.gd`（新）、README、NAVIGATION |
| 2 | 2026-09-09 | 「巨大/变大」道具改造成第六只鸟【长尾山雀】：栖枝预制体（4 帧待机 + 4 帧摔落），消耗那一次左键时鸟从栖枝摔到目标格、趴地震屏后翻开 3×3，随即惊起一下翻着身子掉出屏幕底、再淡回枝头（不等其他道具结算）；红隼模式不收走它；栖枝贴左下角边缘；普通关默认自带 1 张巨大道具（教学关不发），所以第 5 关起常驻左下角树枝。绿幕图由 `tools/cut_bird_sheet.py` 抠成 220×220 标准帧和 256 道具图标。全部「变大」文案改名。 | GameFlow, ShopEconomy, CustomLevel | 长尾山雀（新） | `scenes/birds/tit_bird_perch.tscn`（新）、`my_asset/birds/tit_*.png`（新）、`assets/sprites/generated/bird_items/item_bird_tit.png`（新）、`tools/cut_bird_sheet.py`（新）、`tools/capture_tit_bird.gd`（新）、`scripts/ui/bird_perch.gd`、`scripts/main.gd`、`scenes/main.tscn`、`scripts/ui/shop_overlay.gd`、`scripts/game/custom_level.gd`、`scripts/ui/custom_level_editor.gd`、`tests/test_tit_bird_perch.gd`（新）、`tests/test_tit_bird_crash.gd`（新）、BIRD_PREFAB_STANDARD |
| 2 | 2026-09-10 | 选关图画风改造为「体素陈列柜」：奶油平底 + 正交斜视相机；六边地格只留作数据，运行时铺方格立方体地块（草/沙土侧面/石板广场与路/方格水面，带缺口矩形岛）；图集重配色（草→灰绿、屋顶→陶土）、Blender 生成极简方块树替掉针叶树、收掉草丛；UI 改白卡墨字细边，悬浮牌同套。第二轮按参考图美学再调：曝光压回固有色、相机拉近顶满画框、地块换 16×16 像素贴图 + 顶点色接触阴影、体素图改标准材质平滑受光、广场配楼与外围小屋点缀。第三轮每关一副专属布景（`STAGE_THEMES`：果树坡 / 麦垄田土 / 老井村落 / 双渠双桥 / 潭中栈桥 / 港湾码头），新增田土、木板两种地块。新增 `tools/capture_world_map_direct.gd` 直拍截图。 | WorldMap | 体素地块（新）、石板路（新） | `scripts/ui/world_map.gd`、`scripts/ui/stage_badge.gd`、`shaders/hex_toon*.gdshader`+`hex_toon_body.gdshaderinc`、`assets/hexmap/clean/`、`assets/nature_clean/`、`tools/build_clean_atlas.py`、`tools/build_clean_trees.py`、`tools/capture_world_map_direct.gd`、`tests/test_world_map.gd`、README、NAVIGATION |
| 3 | 2026-09-10 | 长尾山雀补齐三件套：①教学延长为 5 关，第 5 盘（青草坡末盘，仍只发鸟牌）打赢弹长尾山雀解锁页，之后第一盘正式关首次翻开区域内钉一张长尾山雀牌；解锁位写入存档（老存档教学已完成即视为解锁），跳过教学 / 对战 / 自定义一并补发。②解锁页 `tit_unlock`：粉紫底、长尾山雀图案、合成啾声、文案「雪白团子 一坠千金」。③彩蛋：每撩一次立绘就多蹦出一只小长尾山雀，凑够 12 只：全员蹲一下憋气 → 错峰沿弧线飞向中心、拖出「啾」字尾迹 → 撞在一起白光炸开、屏幕一震、咚一声、汤圆碎屑四散 → 碗从底下弹出压扁回弹、之后一直冒热气（碗为 PIL 生成，汤圆是它自己的脸），主文案变「汤 圆」，成就「一碗汤圆」；再撩只晃碗。片尾点唱机鸟群加入第六只。 | GameFlow, Progression, Achievements | 长尾山雀解锁页（新）, 一碗汤圆（新成就） | `scenes/ui/tit_unlock.tscn`（新）、`scripts/ui/tit_unlock.gd`（新）、`my_asset/tangyuan_bowl.png`（新，`tools/build_tangyuan_bowl.py`）、`assets/audio/tit_chirp.wav`（新）、`scripts/main.gd`、`scripts/game/achievement_catalog.gd`、`scripts/ui/credits_bird_swarm.gd`、`tests/test_tit_unlock.gd`（新）、`tests/test_tit_tutorial_unlock.gd`（新）、`tests/test_easter_egg_achievements.gd`、以及把教学长度写死为 4 / 第 5 关当正式关的测试与截图工具 |
| 3 | 2026-09-10 | 修玩家反馈「卡在开始界面、重开进度没了」：每盘开局的进度横幅在分段点动画之后才 `await bar.finished`，帧率偏低（约 50 fps 打 11～12 盘、30 fps 打 ≤8 盘）时进度条 tween 已先结束，await 永远不返回，横幅盖住棋盘不收；改为 tween 还在跑才等。同时把每盘开局的 `_save_progress()` 挪到横幅之前，卡住/中途退出时存档记的是本盘开头而不是上一盘。无头测试用 `Engine.max_fps = 50` 复现并回归。 | GameFlow | 进度横幅 | `scripts/ui/stage_progress_banner.gd`、`scripts/main.gd`、`tests/test_stage_progress_banner.gd` |
| 4 | 2026-09-10 | 新手第 1 盘改成**强引导**：棋盘写死为 5×4 三雷（`GuidedTutorial`，不布随机雷、不发牌），六步逐个放行点击——①左键翻格（连锁翻出左半边）②认数字「周围 8 格的雷数」（知道了按钮）③「不小心」左键点雷、掉 1 点血 ④圈住血条讲生命（知道了按钮）⑤右键标出唯一可能的雷 ⑥左键点数字、由推理自动标出最后一颗雷通关。演出层 `GuidedTutorialOverlay`（CanvasLayer 85）：整屏压暗 + 圆角挖洞（着色器 `tutorial_spotlight`，洞内点击穿透、洞外全拦并弹一下提示）、小手指贴图 `pointer_hand.svg` 指向目标并带「左键/右键」小牌（可镜像到左侧免得挡格子）、RichTextLabel 引导文本卡（强调色 `#d9482b`、步骤角标）。main.gd 只在 `_on_cell_mouse_button_changed` 处按当前步骤放行点击，棋盘每次回到可点状态 `_sync_guided_tutorial()` 检查目标达成。第 1 盘不再显示静态说明图。关卡表第一盘改为 `rect_5x4` / 3 雷。截图工具 `tools/capture_guided_tutorial.gd`。 | GameFlow, BoardModel, WorldMap(关卡表) | 强引导（新） | `scripts/game/guided_tutorial.gd`（新）、`scripts/ui/guided_tutorial_overlay.gd`（新）、`shaders/tutorial_spotlight.gdshader`（新）、`assets/ui/pointer_hand.svg`（新）、`scripts/main.gd`、`scripts/game/minesweeper_board.gd`（`place_fixed_mines`）、`scripts/game/board_shape.gd`、`scripts/game/stage_table.gd`、`tools/capture_guided_tutorial.gd`（新）、`tests/test_guided_tutorial.gd`（新）、`tests/test_roguelite_progression.gd`、`tests/test_stage_run_flow.gd`、`tests/test_board_shape.gd`、README、NAVIGATION |
| 5 | 2026-09-10 | 新手第 2 盘（夜鹭刚解锁）加**伙伴牌课**（`GuidedItemLesson`，复用第 1 盘的遮罩挖洞 / 小手 / 强调色文本卡）：①圈住整盘、左键任意格找出藏在首次翻开区域边缘的夜鹭牌；②牌翻出的那一刻、鸟起飞前停下来（这张牌不预飞），圈住牌讲「伙伴牌翻出即自动生效：夜鹭随机照亮周围 1 个安全格」，按「看它表演」放行结算，引导层让开；③演出完、棋盘回到可点时亮总结步，按「查看伙伴图鉴」弹出 `ItemGuideScreen`（CanvasLayer 86）：奶油底整页，8 位伙伴 4×2 小卡**上图下文**（图标 / 名字 / 作用说明带强调色；夜鹭、红尾水鸲、啄木鸟、红隼、灰喜鹊、长尾山雀、疗愈鸟、透视——「探测」只有自定义模式配得出来，不列），「开始排雷」关掉即课结束、棋盘自由。编排改为按盘选课（`_guided_lesson_for_round()`：1→GuidedTutorial，2→GuidedItemLesson），`_sync_guided_tutorial()` 会连跳已达成的步、并在引导层让开后把当前步亮回来；`_resolve_turn` 在翻牌动画后、结算道具前 `await _guided_item_intro(item_queue)`。第 2 盘不再显示静态说明图。截图工具 `tools/capture_item_lesson.gd`。 | GameFlow | 伙伴图鉴（新） | `scripts/game/guided_item_lesson.gd`（新）、`scripts/ui/item_guide_screen.gd`（新）、`scripts/game/guided_tutorial.gd`（Focus 加 BOARD_ALL/ITEM）、`scripts/ui/guided_tutorial_overlay.gd`（`next_text`）、`scripts/main.gd`、`tools/capture_item_lesson.gd`（新）、`tests/test_guided_item_lesson.gd`（新）、`tests/test_guided_tutorial.gd`、`tests/test_tutorial_skip.gd`（按强引导与教学长度改断言、改用独立存档）、README、NAVIGATION |
| 5 | 2026-09-10 | 「连携」道具改造成第七只鸟【灰喜鹊】：①普通关默认自带 1 张（教学关不发）；②教学延长为 6 关，第 5 盘送长尾山雀后直接开第 6 盘，第 6 盘打赢弹灰喜鹊解锁页；第一盘正式关首次翻开区域里同时钉长尾山雀和灰喜鹊各一张（不同格）；解锁位入存档，老存档教学完成即视为解锁；跳过教学 / 关卡 / 对战 / 自定义一并补发。③连携动画：标出雷后灰喜鹊从屏幕底边栖位飞到落脚格，沿 L 形路线一格一格跳着走、走路四帧轮播（大步 / 眯眼 / 横眼 / 闭眼冒星）、面朝前进方向、每步棋盘轻震，走到哪格翻哪格（`_resolve_reveal_targets` 新增 `walker` 回调），到头冲雷横一眼再淡出、回栖位。④解锁页 `magpie_unlock`：灰蓝底，每撩一次整页虚焦加深（屏幕纹理 mip 模糊），撩 6 次一副比屏幕大得多的黑框眼镜（4.5 倍，3040 px 抗锯齿贴图）从右甩入，只有右镜片留在画面里、正压在鸟眼上，镜片里对焦出全分辨率的黑长直学姐（镜片遮罩 shader），成就「幻视学姐」；再撩只晃镜片。继续按钮提到虚焦层之上，始终清晰。素材：绿幕图 `tools/cut_bird_sheet.py` 切帧，`tools/build_magpie_assets.py` 抠学姐、画眼镜、合成叫声。片尾鸟群第七只。 | GameFlow, Progression, Achievements, ShopEconomy, CustomLevel | 灰喜鹊（新）, 幻视学姐（新成就） | `scenes/birds/magpie_bird_perch.tscn`（新）、`my_asset/birds/magpie_*.png`（新）、`my_asset/magpie_senpai*.png`、`my_asset/magpie_glasses*.png`（新）、`assets/audio/magpie_call.wav`（新）、`shaders/unlock_defocus.gdshader`、`shaders/lens_mask.gdshader`（新）、`scenes/ui/magpie_unlock.tscn`、`scripts/ui/magpie_unlock.gd`（新）、`scripts/ui/bird_perch.gd`、`scripts/main.gd`、`scenes/main.tscn`、`scripts/game/stage_table.gd`、`scripts/game/achievement_catalog.gd`、`scripts/ui/shop_overlay.gd`、`scripts/game/custom_level.gd`、`scripts/ui/custom_level_editor.gd`、`scripts/ui/credits_bird_swarm.gd`、`tests/test_magpie_*.gd`（新 4 条）、`tools/capture_magpie_*.gd`（新）、以及把教学长度写死为 5 的测试与截图工具 |
| 6 | 2026-09-10 | 选关图放一只**蚊子**：白底 4×2 图集抠成透明图集（边框泛洪抠白、眼白保留），在画面里带抖动地乱飞（顶栏下方、四边留边），翅膀 8 帧轮播、嗡嗡声离指针越近越响；鼠标**慢慢**挪过去它反应一下就窜开，**快挥**（≥900 px/s，按两帧指针线段扫过身子判定）才拍死：啪一声、压扁、翻着圈掉下去、弹出「啪！」和几粒碎屑，7～14 秒后再飞进来一只。整层不吃鼠标，不挡悬浮牌与按钮；present/dismiss 起停。 | WorldMap | 蚊子（新） | `scripts/ui/map_mosquito.gd`（新）、`my_asset/source/mosquito_sheet.png`（新）、`assets/sprites/generated/mosquito/mosquito_sheet.png`（新）、`assets/audio/mosquito_buzz.wav`、`assets/audio/mosquito_swat.wav`（新，合成）、`tools/build_mosquito_assets.py`（新）、`scripts/ui/world_map.gd`、`tools/capture_world_map_direct.gd`、`tests/test_map_mosquito.gd`（新）、`tests/test_world_map.gd`、README、NAVIGATION |
| 7 | 2026-09-11 | 选关图新方向的**试点展品**：第一关「青草坡」做成一件独立的低模场景艺术品（不再在一张大地图上堆六个主题）。Blender 无头脚本从 ASCII 掩码生成三级台地（上层台地 / 下层果园 / 后侧小丘）与切入的溪流、阶梯悬崖两层地层、顶面贴图（棋盘草地 + 软边沙径 + 接触阴影）；定制道具：瓦片屋顶小屋（逐片瓦、百叶窗、花箱、烟囱）、鸽舍塔、草垛、方块果树与松树、石板小广场与鸟浴盆（关卡锚点）、木桥、篱栏、阶梯、菜畦、柴堆、鸟屋、路牌、白鸽、溪口浪花。全部倒角 + 按角度平滑、哑光柔和配色、按组合并导出单个 GLB。Godot 端 `StageDiorama` 负责展柜：奶油渐变台面 + 岛底软阴影贴花、主光（左后上）/ 低角度前补光 / 环境光、卡通溪水面、正交展柜相机；布光按实测标定（Compatibility 渲染器里主光与环境光叠加不是线性相加，靠量像素校到顶面 ≈1.0× 固有色、朝观众的悬崖面 ≈0.6×）。`tools/capture_diorama.gd` 出 1920×1080 过目图；`DIORAMA_LIVE=1` 直读 .glb 免导入迭代。尚未接入选关流程。 | WorldMap | 展品/diorama（新） | `tools/diorama_kit.py`（新）、`tools/build_diorama_grass_1.py`（新）、`assets/dioramas/grass_1/`（新，GLB+JSON）、`scripts/ui/stage_diorama.gd`（新）、`scenes/dioramas/grass_1.tscn`（新）、`tools/capture_diorama.gd`（新）、`tests/test_stage_diorama.gd`（新）、NAVIGATION、README |
| 8 | 2026-09-11 | 展品两项：①台面换成着色器波点桌布（`diorama_table.gdshader`）：屏幕空间正圆点、错排六角网格、沙/灰绿/陶土三色低对比点子按格哈希分配、上下渐变 + 暗角、点阵极慢漂移；仍是受光平面，岛影照落。②「搭积木」入场（`diorama_build.gdshader`）：Blender 端每件道具是一个 piece，基点与起跳时刻写进顶点色（rgb 基点归一化、a 时刻），Godot 只推进 `build` 一个数；地形先从台面弹起（只动 y），随后石板一圈圈涟漪铺开、小屋先起墙再落顶、树逐棵弹出、花草再冒、鸽子最后落，按后左→前右扫掠加抖动，阻尼弹簧过冲、竖向多弹一点像果冻；溪水在地台落定后从河床升到水位，岛影同步淡入；全程约 1.9 秒。`StageDiorama.play_entrance()/skip_entrance()`、`entrance_finished` 信号、`entrance_on_ready`。截图工具 `DIORAMA_ENTRANCE=1` 可按 `DIORAMA_FRAMES` 时刻另存过程帧。测试新增顶点色基点解码抽查与入场推进检查。 | WorldMap | 波点桌布（新）、搭积木入场（新） | `shaders/diorama_table.gdshader`（新）、`shaders/diorama_build.gdshader`（新）、`tools/diorama_kit.py`、`tools/build_diorama_grass_1.py`、`scripts/ui/stage_diorama.gd`、`tools/capture_diorama.gd`、`tests/test_stage_diorama.gd`、NAVIGATION |
| 9 | 2026-09-12 | 新增第 7 关【无尽远海】（无尽随机、难度逐步增加）：关卡表加 `endless` 条目（无盘列表、`target_round` 为 0），每盘由 `StageTable.endless_board(round, seed)` 按 (盘序, 本盘种子) 从全部形状族里抽形状——盘面每 3 盘放大一号（7 → 10 封顶），雷密度从 0.13 每盘 +0.008 爬到 0.28 封顶，雷数夹在 4 与形状雷上限之间；同一局内可复现。流程上 `_stage_complete()` 对无尽关恒为假，只有血量归零才收场：倒下时弹「止步第 N 盘」结算榜（复用通关榜页，本局累计分上榜），回柜子停在无尽关。打赢一盘刷新「最远第几盘」并落盘（存档 v4 `endless_best_round`，放弃本轮也保留）。柜子牌子芯片写「无尽」、记录行「最远 N 盘 · 最高 X」，关卡卡「无尽 · 越打越难 · 最远第 N 盘」，续局条「第 N 盘 · 无尽」，地形区读数不把无尽关算进总数；盘内进度横幅改成「第 N / ∞ 盘」、进度条画难度爬升、不摆分段点；盘序读数「第 N 盘 · 无尽」。展品沿用灰泥胚占位。截图 `tools/capture_cabinet_endless.gd`。 | WorldMap(关卡表), GameFlow, Persistence | 无尽关（新） | `scripts/game/stage_table.gd`、`scripts/main.gd`、`scripts/game/game_save.gd`、`scripts/ui/stage_badge.gd`、`scripts/ui/stage_cabinet.gd`、`scripts/ui/stage_progress_banner.gd`、`scripts/ui/leaderboard_panel.gd`、`tools/capture_cabinet_endless.gd`（新）、`tests/test_endless_stage.gd`（新）、`tests/test_stage_table.gd`、`tests/test_board_shape.gd`、`tests/test_stage_cabinet.gd`、README、NAVIGATION |
| 10 | 2026-09-12 | 六关展品**拉开差距**（用户：房子太多、太同质化）：每关一个别处没有的主景与地形轮廓，不再是「草地上摆小屋换屋顶色」。老井村改成三级石头梯田：干砌石墙沿台沿、石阶连台、方形石塔、长条低矮石屋（石砌、小深窗、无花箱）、井亭（四柱两重板岩顶，锚点）与一棵挂红绸的古树、石畜栏与石槽、右侧菜畦台地；双桥改成无民居的水园：河在岛心汇成潭、潭中小岛立六角亭（朱柱、两重板岩顶、金顶珠，锚点），两座朱红拱桥从两岸错行搭上岛，石灯笼、垂柳、鸢尾、睡莲、锦鲤、观景木台与踏石，唯一的房子是后侧水磨（石基 + 木板上层 + 阁楼门吊梁，轮子改成轮圈 + 辐条 + 桨板）；尽头港改成有防波堤的作业港：石堤从右岸弯出合围港池、灯塔立在堤尖礁石上，岸上只剩一座长条船库（灰蓝木板、藏青大门、圆窗、救生圈、烟管）和一座带阳台旗杆的高窄港务塔，码头吊车、渔网晾架、龟笼、系船柱、沙滩上拉上岸的小船。顺手修了一个老 bug：`Kit.finalize` 的 bmesh 合并丢了 UV，导致所有手绘地面（石板、沙滩、麦垄条纹、沙径）在引擎里只剩棋盘底色；现在合并时把每个部件的 UV 层一并写入，测试里新增「带贴图的面必须有 UV」。深潭补齐岛外道具、瀑布水帘改淡青。 | WorldMap | 古树 / 井亭 / 石塔 / 干砌石墙 / 六角亭 / 石灯笼 / 观景台 / 船库 / 港务塔 / 吊车 / 晾网架 / 系船柱 / 龟笼（新道具） | `tools/diorama_props.py`、`tools/diorama_kit.py`（`torus`、UV 合并、`steps` 支持起始高度与竖向墙）、`tools/build_diorama_grass_3.py`、`tools/build_diorama_river_1.py`、`tools/build_diorama_coast_1.py`（重写）、`tools/build_diorama_river_2.py`、`assets/dioramas/*`（六关重建）、`tools/dump_diorama_texture.gd`（新，贴图核对）、`tests/test_stage_dioramas.gd`、FEAT-005、NAVIGATION、README |
| 11 | 2026-09-12 | 展品**活起来 + 荒废态**。①常态微动全在着色器里：Blender 端每件东西的 piece 带一个运动种类与相位，写进第二套 UV（TEXCOORD_1），`diorama_build.gdshader` 按种类围着该件的基点动——枝叶越高摆得越多、停着的鸟点头转身、船与浮标随波摇晃、风车翼与水车轮转、挂着的红绸/渔网/吊车货箱小钟摆、烟囱冒炊烟（一团团升起、先长大再散）、旗子抖、鸽群绕圈飞（每关上空一圈 3～4 只）、锦鲤绕圈游、睡莲和浪花浮动；零 CPU 开销。②云：每件展品上空 3 朵卡通云沿画面横向漂、飘出航线再从另一头回来；云影一份是只投影的椭球交给主光真阴影落在岛与道具上，一份塞进水着色器的 `cloud_shadows` 画在水面上（水面不吃灯光阴影）。③荒废态：每关按同一布景脚本用 `DIORAMA_WITHERED=1` 再出一版 `<id>_withered.glb/.json`——不是只换草色：配色整体变干变灰（`wither_hex`，绿→枯黄、饱和度砍一半、往灰米色靠），树只剩枯干与枯枝、柳条光秃、松树成褐色枯杆、花只剩枯梗、灌木塌成一团；窗户钉木板、门洞黑、瓦片缺一成、烟囱帽掉在地上、墙上爬常春藤、没有炊烟；风车翼断一根撕两张、水车缺半数桨板不转、井亭上重顶塌掉井水干成泥、六角亭上重顶塌栏杆断、拱桥缺板少一侧栏、栈桥断一截桩子歪、船在水里半沉侧倾在沙滩上翻扣、浮标熄灯倾斜、灯塔灯灭栏杆断塔帽歪、船库一扇门没了救生圈没了屋面吹掉一截、港务塔阳台栏断三角旗成破布、吊车臂垂落绳子空着、晾网只剩破网、篱栏杆掉地、干砌石墙塌块、板车翻倒轮子掉、稻草人歪倒没帽、麦捆倒地、草垛塌成饼、木桶横躺、木箱摔开、灯柱与石灯笼全熄；水位下降变浑（双桥、深潭），溪流与台地小溪干成裂纹泥（青草坡、深潭），海水变灰绿；鸟全部飞走，只留一只乌鸦；云偏灰暗；桌布随之变灰。④柜子：没通关的关摆荒废版（未解锁的再叠泥胚抽色），通关记录里有的摆生机版；刚通关回到柜子时先停在那关——荒废版按原路收回、生机版重搭（复原，鸟群回来、炊烟升起），停一下再自动滑到下一关。 | WorldMap | 常态微动（新）、云与云影（新）、荒废态 / 复原（新） | `tools/diorama_kit.py`（piece motion、Motion UV 层、GroundPainter tint）、`tools/diorama_props.py`（重写：运动种类、withered 分支、`wither_hex/wither_palette/wither_table`、炊烟、飞鸟、乌鸦）、`tools/build_diorama_*.py`（变体、干涸/浑水、飞鸟）、`shaders/diorama_build.gdshader`（UV2 微动）、`scripts/ui/stage_diorama.gd`（withered、set_withered 复原、云）、`scripts/ui/stage_cabinet.gd`（按通关摆版、复原后自动前进）、`tools/capture_stages.gd`（DIORAMA_WITHERED）、`tests/test_stage_dioramas.gd`、`tests/test_stage_cabinet.gd`、`assets/dioramas/*`（12 版）、FEAT-005、NAVIGATION、README |
| 12 | 2026-09-12 | 通关后的固定流程：①弹排行榜结算页（原有）→ ②回柜子播**复原仪式**：柜子在榜还开着（`modal_overlay` 可见）时先停在刚通关那关、保持荒废版并记下待播，榜一关（或任何 modal 收掉）`_process` 起播——荒废版按原路收回；生机版重搭那一刻台面上一圈暖光扩开、整座岛升起叶片花瓣（方片、按叶色/花瓣色随机、慢慢飘落）与暖光点（加法混合、往上浮），鸟落回锚点时再迸一团星屑；UI 上弹标题卡「「关名」复苏 · 荒芜褪去 · 生机回来了」，随后按时刻弹三行关卡专属短句（青草坡「草坡返青 / 果树重新挂果 / 鸽群归来」、麦垄「麦浪再起 / 风车重新转动 / 谷仓门修好了」、老井村「古树重新抽叶 / 井水复清 / 红绸再挂上枝头」、双桥「清流回潭 / 水车再转 / 锦鲤回来了」、深潭「瀑布复流 / 松林返青 / 潭水又深了」、尽头港「灯塔重亮 / 船队归港 / 海鸟归来」），往上飘一点再淡掉；仪式期间不接受进关与切换 → ③标题卡留到最后一句说完（`RESTORE_TAIL`）再淡出，停 1.7 s 自动滑到下一关（已解锁且未通关时）。整段仪式的节奏按用户「特效稍微放慢」又缓了一档：收回、光圈、花瓣光点星屑的寿命与初速、标题与短句的进出停都拉长，展品入场本身不变。主场景流程不用改：`_on_stage_cleared` 先 present 再开榜、关榜再 present，柜子自己记着待播的复原。 | WorldMap, GameFlow | 复原仪式（新） | `scripts/ui/stage_cabinet.gd`（待播/起播/文字反馈/守卫）、`scripts/ui/stage_diorama.gd`（`_spawn_restore_effects` 粒子）、`tools/capture_restore.gd`（新，连拍仪式）、`tests/test_stage_cabinet.gd`（榜开着不播、再 present 不丢、粒子与短句、自动前进）、FEAT-005、NAVIGATION、README |
| 13 | 2026-09-12 | **把开幕剧情与耳机提示加回来**。两段演出（`headphone_notice` 五行砸字 + `opening_story` 四页插画）一直都在，断的是触发条件：FEAT-002 把判据从「这一局还没开始」改成「地图上一片空白」（`_cleared_stages.is_empty()`），于是只要通关过任何一关就再也看不到开场。现在按落盘的 `intro_played` 走（照 `music_break_played` 的先例）：没放过就放、放完记账落盘，回标题再开始不重放；老档没有这个键、读出来是 false，所以**手上有进度的存档会补放一次**。「放弃本轮」保留这个标记（放弃的是半局进度，不该把开场再演一遍），清空存档则连同标记一起回到未放过。两段演出本来就能跳过（耳机提示任意键/点击，开场剧情长按）。 | GameFlow, Persistence | — | `scripts/main.gd`（`_intro_played` 的声明/读/写/重置与触发条件）、`scripts/game/game_save.gd`（键说明）、`tests/test_opening_intro_gate.gd`（新，专盯这次回归：有进度的存档补放、记账落盘、第二次不重放）、`tests/test_stage_run_flow.gd`（跳过开场改用新判据）、README |
| 14 | 2026-09-12 | 关卡选择界面**不再漏出**灰喜鹊与长尾山雀的栖枝：`SCENERY_PLAY_NODES` 当初只列了前五只鸟，新加的两只没跟上，`_set_scenery_visible(false)` 收不走它们，于是陈列柜选关时两只鸟一直挂在左下角。两个节点名补进表里即可。 | GameFlow | 灰喜鹊、长尾山雀 | `scripts/main.gd` |
| 15 | 2026-09-12 | 灰喜鹊与长尾山雀**放大**（0.62 → 0.78），和其他几只鸟（0.9）的视觉分量拉齐；灰喜鹊同时右移 40px、上移 35px，免得放大后两只互相压住、尾巴掉出屏幕底。尺寸按贴图 alpha 包围盒量过。 | GameFlow | 灰喜鹊、长尾山雀 | `scenes/main.tscn` |
| 16 | 2026-09-12 | 长尾山雀的 3×3 **踩到雷改为标记、不再引爆**（和啄木鸟清线同一个规矩）：`_resolve_reveal_targets()` 新增 `flag_mines` 通道，命中的雷走 `mark_mine()` + 正确标记反馈 + `_attack_newly_flagged_smalls()`，剩余雷读数与连击照常结算。道具提示 / 商店 / 伙伴图鉴文案同步。顺带把 `test_enlarge_invincible` 的选格改成「3×3 内没有道具牌」，否则疗愈鸟回血或再翻出一张山雀会把断言带偏（这正是它长期 flaky 的原因）。 | GameFlow, ShopEconomy | 长尾山雀 | `scripts/main.gd`、`scripts/ui/shop_overlay.gd`、`scripts/game/guided_item_lesson.gd`、`tests/test_enlarge_invincible.gd` |
| 17 | 2026-09-12 | **连携规则改造：连携雷**。旧规则（灰喜鹊道具牌落脚 A → 下一颗标出的雷 B → 走 L 形路径踩开沿途格子）整套下线，改成：开局布雷时就从雷里挑出一组连携雷（`CHAIN_COUNT = 2` + 商店加成，与布雷共用 `_rng` 所以对战双方一致），开局对玩家不可见；玩家标中组里任意一颗，灰喜鹊飞过来把同组剩下的雷逐颗**标记**出来（不是引爆），计分与连击照常。`ItemType.CHAIN` 不再作为道具牌埋进草地（`configured_item_count()` 恒为 0），棋盘新增 `is_chain_mine()` / `chain_mine_total()` / `covered_chain_mines()`。 | GameFlow, ShopEconomy, CustomLevel | 连携雷（新）、灰喜鹊 | `scripts/game/minesweeper_board.gd`、`scripts/main.gd`、`scripts/ui/shop_overlay.gd`、`scripts/game/guided_item_lesson.gd`、`scripts/game/custom_level.gd`、`tests/test_chain_mine_link.gd`（新，替换 `test_magpie_chain_walk.gd`）、`tests/test_new_tactical_items.gd`、`tests/test_tactical_shop_inventory.gd`、`tests/test_magpie_tutorial_unlock.gd`、BIRD_PREFAB_STANDARD |
| 18 | 2026-09-12 | 每盘**开局演出**：先洗牌发牌（所有草地牌叠在棋盘中心，按离牌堆的距离一张张甩到各自格子，`MineCell.prepare_deal()` / `play_deal_in()`），再亮一次「这一盘的草地下」横幅，列出雷、连携雷和每一种埋着的伙伴牌及数量（`scripts/ui/round_intro_banner.gd`，摆在棋盘下沿那条空木板带上，不压读数行）。演完才把棋盘交还玩家。强引导那一盘和对战跳过这段。棋盘上方常驻的「剩余/总数」读数条保留不动。 | GameFlow | 开局横幅（新）、洗牌发牌（新） | `scripts/ui/round_intro_banner.gd`（新）、`scripts/ui/mine_cell.gd`、`scripts/main.gd`、`tools/capture_round_intro.gd`（新）、`tests/test_round_intro.gd`（新） |
| 19 | 2026-09-12 | 「透视」道具改造成第八只鸟【小嘴乌鸦】：①普通关默认仍是 1 张（教学关不发）；②教学延长为 7 关，第 5 盘送长尾山雀、第 6 盘送灰喜鹊都改成点继续直接开下一盘，第 7 盘打赢弹小嘴乌鸦解锁页；第一盘正式关首次翻开区域里同时钉长尾山雀和小嘴乌鸦各一张（不同格）；解锁位入存档，老存档教学完成即视为解锁；跳过教学 / 关卡 / 对战 / 自定义一并补发。③透视动画：乌鸦**缩到一格大小**（BirdPerch 新增 `board_travel_scale`，灰喜鹊同样缩）飞到随机那一格**旁边**（栖位上的鸟有三格宽，压在格子上会把要给玩家看的内容盖住），掀起卡片一角偷看（帧 5 探身、帧 6 掀开）；内容亮 3 秒但不翻开、这几秒棋盘不锁，鸟不陪看——顿一拍（帧 7 被自己掀出来的东西吓一跳）、叫一声、冒个圈就地消失，回落脚地。④解锁页 `crow_unlock`：暗橄榄底，文案「小嘴乌鸦 / 薅小猫毛」。彩蛋：页脚躺着一只睡熟的动物，撩一次薅走一撮它自己颜色的毛、换下一只躺过来，副标题跟着改口——薅小猫毛 → 小狗 → 小羊 → 小马 → 小鹿 → 狐狸 → 熊猫；薅到熊猫就翻脸，一鸟一熊绕着整个屏幕跑两圈，跑完乌鸦灰溜溜回原位、熊猫在旁边坐下不走，副标题变「薅错了」，成就「薅到熊猫头上」。素材：绿幕图 `tools/cut_bird_sheet.py` 切帧（新增「孤立数字＝帧号」规则，认得左上角的裸编号），七只动物 + 翻脸熊猫 + 飞毛 + 叫声由 `tools/build_crow_assets.py` 生成。片尾鸟群第八只。 | GameFlow, Progression, Achievements, ShopEconomy, CustomLevel | 小嘴乌鸦（新）, 薅到熊猫头上（新成就） | `scenes/birds/crow_bird_perch.tscn`（新）、`my_asset/birds/crow_*.png`（新）、`my_asset/crow_victim_*.png`（新）、`my_asset/crow_fur_*.png`（新）、`assets/audio/crow_call.wav`、`crow_animal_yelp.wav`（新）、`scenes/ui/crow_unlock.tscn`、`scripts/ui/crow_unlock.gd`（新）、`tools/build_crow_assets.py`（新）、`tools/cut_bird_sheet.py`、`scripts/ui/bird_perch.gd`（新增 `art_faces_right`，乌鸦素材朝左）、`scripts/main.gd`、`scenes/main.tscn`、`scripts/game/stage_table.gd`、`scripts/game/achievement_catalog.gd`、`scripts/ui/shop_overlay.gd`、`scripts/game/custom_level.gd`、`scripts/ui/custom_level_editor.gd`、`scripts/ui/credits_bird_swarm.gd`、`tests/test_crow_*.gd`（新 4 条）、`tools/capture_crow_*.gd`（新）、以及把教学长度写死为 6 的测试与截图工具 |
| 20 | 2026-09-12 | 两个演出修正：①**连携改成一雷一鸟**——从前是栖枝上那一只飞下来挨个跑，雷一多就要排队等很久，半空中还是一张不动的图；现在同组每颗雷各派一只灰喜鹊分身，全部从刚标出的那格同时出发、错开 0.07 秒、沿各自的弧线飞到自己的目标，翅膀按 0.09 秒一帧扇（帧号按真实时间算，所以飞行 tween 必须线性，一挂缓动翅膀就会在两头慢下来），落地收翅、飘起淡出；栖枝上那只留在原地只叫一声。分身尺寸沿用 `board_travel_scale`，还是一格大小。②**透视看起来像透视了**——闪烁一直都在跑，但只是在完好的草地牌面上飘一个小数字，牌面不动就读不出「看穿」；现在草皮自己反相淡到两成、底下的内容同时亮到满，一呼一吸，数字也从 38 号放大到 46 号。 | GameFlow, MineCell | 连携雷, 透视 | `scripts/main.gd`、`scripts/ui/mine_cell.gd`、`tests/test_chain_mine_link.gd`、`tools/capture_magpie_walk.gd`（按新流程重写，旧的还在按「连携是道具牌」跑，早就失效了） |
| 14 | 2026-09-12 | 选关图的蚊子**拍满三只收手**：第 3 只拍死的那一下发成就【三振出局】（关卡选择界面，一口气拍死 3 只蚊子），同时这只蚊子彻底退场——不再排下一只，回选关、通关复原都会调的 `start()` 也叫不回来（`MapMosquito.SWAT_LIMIT = 3`、`is_retired()`）。退场闸做在 `_tick` 的 HIDDEN 分支里而不是只靠 `set_process(false)`，否则截图工具与测试手动推帧时它还会重生。成就图标是从蚊子图集里挑翅膀张得最开的一格裁出来的（`build_mosquito_assets.py` 新增 `build_icon()`）。顺手修了一个存档隐患：`tests/test_achievements_screen.gd` 跑主场景、调两次 `_start_game()` 却没隔离存档路径，会把玩家真存档覆盖成一局新的，现在给它自己的 `user://` 槽并加了自超时。 | WorldMap, Achievements | 三振出局（新成就） | `scripts/ui/map_mosquito.gd`、`scripts/ui/stage_cabinet.gd`（连 swatted、发成就、`mosquito()` 探针）、`scripts/game/achievement_catalog.gd`、`tools/build_mosquito_assets.py`、`assets/sprites/generated/mosquito/mosquito_icon.png`（新）、`tests/test_map_mosquito.gd`、`tests/test_stage_cabinet.gd`、`tests/test_achievements_screen.gd` |
| 21 | 2026-09-12 | 标题页新增【鸟类图鉴】页面：次级入口那一排多一颗按钮（挤窄到 236 宽，不另占一行——菜单列已经从标题排到屏幕底），点开是一张大卡，把队里每只鸟铺成两行小卡。每张卡上图下文：头像播的就是栖枝预制体那套 220×220 待机帧（照 BirdPerch 的 `[0,1,2,3,2,1]` 节奏，每张卡相位错开 0.17 秒，八只不会一起抬头；乌鸦的图朝左画，按 `faces_right` 翻回来），下面是名字、外号和一句能力说明，底部一枚「已加入 / 未解锁」的牌子。没解锁的只露黑剪影、名字打码成「？ ？ ？」、能力那行换成解锁条件，卡片本身也换成压暗的底——图鉴因此同时是一张「还差谁」的清单，右上角记「已收录 N/8」。解锁状态不另记账：总表 `BirdCatalog` 只管文案与帧，`main.gd` 的 `_bird_unlock_states()` 把那一排 `*_unlocked` 映射过去，刷新挂在 `_apply_bird_unlock_visibility()` 最前面（挂在对战早退分支之前，送鸟 / 读盘 / 跳过教学的每条路都会带上它）。页面两行铺满、列数按鸟数算（8 只 → 4 列，9 只 → 5 列），加鸟不用动排版。 | GameFlow, Persistence | 鸟类图鉴（新） | `scripts/game/bird_catalog.gd`（新）、`scripts/ui/bird_codex_screen.gd`（新）、`scripts/ui/start_screen.gd`、`scripts/main.gd`、`tests/test_bird_codex.gd`（新）、`tools/capture_bird_codex.gd`（新）、README、BIRD_PREFAB_STANDARD、NAVIGATION |
| 22 | 2026-09-12 | 「治愈」道具改造成第九只鸟【斑鸠】：栖枝预制体（3 帧待机 + 4 帧动作）、翻出那张牌时它缩小飞过去把叼着的树枝捡起来、再飞到血条边把枝递过去，**爱心在它抵达的那一刻才回**（奖励落在看得见的落点上）；教学收尾关（第 8 盘）送它，解锁页彩蛋是「很随便地叼来树枝筑巢」——每撩一次丢一根角度全凭心情的枝，堆到第 7 根红隼从右边俯冲进来把它叼走、副标题当场改成「好 吃」，成就 `dove_snatched`。顺手修了胜利分支的关卡阶梯：原来第 5 盘谁都不发、小嘴乌鸦那条分支是死代码、最后一盘又弹了一遍乌鸦页；现在山雀=5、灰喜鹊=6、乌鸦=7、斑鸠=8。**关内九只鸟重排**：统一的是「视觉体量」——每帧不透明像素数的平方根（195px），不是外接框的长边；按长边归一会把拖长尾的撑大、实心的压小，九只的实际体量从 167 散到 229，小蓝鸟和啄木鸟就是被撑大的那两只。两条边条各排一列、x 左右交错成前后有层次的一棵树，每列**从下往上**排，最后一只的脚压在画面下沿，富余留在头顶；里档按最宽的那只（红尾水鸲 332px）定，否则尾巴会伸到棋盘上。每只鸟脚下的树枝由它的画框算出来而不是手摆的。位置一律走**锚点**不走绝对坐标——工程是 `stretch/aspect=expand`，窗口换画幅视口就跟着变，写死坐标就不贴边了（2400×1080 实测零偏移）。棋盘实测最大到 10×10、格子占 x 477..1441 / y 78..1041，底下只剩 39px，所以正下方站不下鸟。 | GameFlow, ShopEconomy, CustomLevel | 斑鸠（新） | `scenes/birds/dove_bird_perch.tscn`（新）、`scenes/ui/dove_unlock.tscn`（新）、`scripts/ui/dove_unlock.gd`（新）、`my_asset/birds/dove_*.png`（新）、`my_asset/dove_twig.png`（新）、`assets/audio/dove_coo.wav`（新）、`tools/build_dove_assets.py`（新）、`tools/capture_dove_heal.gd`（新）、`tools/capture_dove_unlock.gd`（新）、`scripts/main.gd`、`scripts/game/achievement_catalog.gd`、`scenes/main.tscn`、九个 `scenes/birds/*_bird_perch.tscn`、`tests/test_dove_{bird_perch,heal,unlock,tutorial_unlock}.gd`（新）、`tests/test_{tit,magpie,crow}_tutorial_unlock.gd`、`tests/test_easter_egg_achievements.gd`、`tests/test_credits_bird_swarm.gd`、BIRD_PREFAB_STANDARD |
| 23 | 2026-09-13 | 商店与经济收紧四件事：①**上限商品**——红隼最多 3 只、长尾山雀最多 2 张（含每关自带的那一张），买满就整轮 run 下架，抽取池里摘掉、货架上只留「已满」；②**重复购买涨价**——同一件商品第 n 次的价格 = 基础价 5G × 1.2ⁿ 向上取整（5/6/8/9/11…），次数逐件记并写进存档，刷新价仍是 5G；商店因此改成按件显示单价、按件判断买不买得起，同一排里便宜的那件不再被贵货连坐禁用；③**单关通关奖励 +3G**，与标雷、吃鱼、连击分行结算；④**账单重画成小票**：锯齿撕边的纸、店名抬头与单号、「品名／数量／金额」三栏用点线连起来、小计与余额之间压一道双实线、左下角盖「已结清」的章，金额为 0 的行不印。外加单机血条**叠放**：上限超过 4 颗后并成最多 4 摞、每摞最多 3 颗（6 → 2+2+1+1），整排排不下就等比缩小，绝不越出叶子底板。 | ShopEconomy, GameFlow, Persistence | 上限商品（新）、重复购买涨价（新）、通关奖励（新）、心叠放（新） | `scripts/main.gd`、`scripts/ui/shop_overlay.gd`、`scripts/ui/level_bill.gd`、`scenes/ui/level_bill.tscn`、`scripts/ui/player_status.gd`、`scenes/player_status.tscn`、`tools/capture_level_bill.gd`（新）、`tests/test_shop_economy.gd`（新）、`tests/test_level_bill_receipt.gd`（新）、`tests/test_heart_stacks.gd`（新）、`tests/test_level_clear_reward.gd`（新）、`tests/test_roguelite_progression.gd`、NAVIGATION |
| 24 | 2026-09-15 | 无尽关的商店改成**槽位制（鸟窝）**：一格住一只伙伴，每盘按窝里的鸟往棋盘上埋牌。起手 6 格，`_prepare_stage_run()` 送的四只（夜鹭 / 红尾水鸲 / 啄木鸟 / 红隼）直接入住，所以开局就是 4 / 6。窝里有空格就直接住进去；**窝满了再买同类伙伴，得先点一格说换掉谁**——按下商品的那一刻不扣钱，进入替换态（整排货、继续、刷新全锁住，只剩「换」或「算了」），点了格子才结账，取消则一分不花。**扩建鸟窝**：一次 +2 格、最多三次、12 / 22 / 36 G，扩到头按钮收起；窝最大 12 格。商店里多出一条鸟窝面板（货架下方那条空纸带），逐格画出住客的头像与短名，空格写「空」，悬停讲这只鸟干什么、替换态讲点下去会换掉谁。实现上**鸟窝是那七个 `*_bonus` 的唯一真相**（`SLOT_OFFER_BONUS` 表 + `_sync_bonuses_from_slots()` 照窝重算），所以替换和撤下永远不会把数值算歪；无尽关因此也不再叠红隼 3 只 / 长尾山雀 2 张那层单品上限，格子就是上限。强化类商品（血量上限、无敌步数、十字啄击、随机格数、标记格数、斑鸠回血）不占格子，照旧无限买。鸟窝随存档往返（v5 新增 `endless_slots` / `endless_slot_expansions`），续无尽关时以窝为准重算数值位；别的关卡与对战完全看不到这套 UI。 | ShopEconomy, GameFlow, Persistence | 鸟窝（新）、槽位替换（新）、扩建鸟窝（新） | `scripts/main.gd`、`scripts/ui/shop_overlay.gd`、`scripts/game/game_save.gd`、`tools/capture_shop_slots.gd`（新）、`tests/test_endless_slot_shop.gd`（新）、README、NAVIGATION |
| 25 | 2026-09-15 | 同一盘大量触发道具的优化：**奖励盘**。以前一盘的雷被提前清空之后，结算队列里剩下的伙伴牌全部落空（`_check_victory_now()` 被 `_item_queue_dispatching` 拖着，牌照发不误，却无处可落）。现在队列每取下一张牌之前先看一眼：本盘的雷已经用尽、队列却还压着牌，就自动展开一张**同尺寸同掩码同雷数**的奖励盘——**不发任何新牌**（这是硬要求，否则队列会自己养自己、一盘接一盘停不下来），只把队列里剩下的那几张原样种回同一格并翻开（原格被雷占了就就近挪一格，队列与去重表同步改写），剩下的牌在它上面照常结算，结算完这一盘就收场、照常进本该进的下一盘。演出：整盘压成暖金（`BONUS_BOARD_TINT`）、复用开局那套洗牌把牌一张张发下去，外加 `BonusBoardAura`——贴着棋盘外框跑一圈的流光、边上飘的星火与顶上「奖 励 盘 / 道具继续结算」的标题牌，全是代码绘制不加素材。规则上它只赚不赔：奖励盘期间 `_is_invincible()` 恒真（翻出雷只标记不掉血，HUD 不挂那个没有倒计时的「无敌」角标），盘上标出的雷照常算金币与得分——旧盘那几笔先记进 `_banked_flagged_mines` / `_banked_scored_mines`，账单把前后几块加起来，不会被新盘顶掉。一盘最多连开 3 张；对战、自定义包、教学段一律不开（各有各的换盘节奏，混在一起只会打架）。 | GameFlow, BoardModel | 奖励盘（新） | `scripts/main.gd`、`scripts/ui/bonus_board_aura.gd`（新）、`tools/capture_bonus_board.gd`（新）、`tests/test_bonus_board.gd`（新）、README、NAVIGATION |
| 26 | 2026-09-16 | 奖励盘的两处修正 + 换鸟改成中央弹窗。①**奖励盘一直没被顶出来**：判据原先卡在「队列非空」，可派发是固定 0.14 秒一张、不等结果的，一大把牌会在一秒内全部离队，而盘面要到一两秒后才变成「已赢」——那时队列早空了，于是剩下的牌一张张空转（红尾水鸲「没找到雷」），然后本盘收场、摊开显示雷，玩家看到的就是「不播鸟动画、直接显示雷」。判据改到**每张牌真正生效之前**（`_resolve_queued_item()` 里 `match` 之前那一道），派发处那道只留作省一张牌的优化。多张牌可能同时撞上换盘，用 `_request_bonus_board()` 合流：先到的换，其余的等同一张盘；等「在跑效果的牌」落地时要把**停在闸前的牌**减掉（`_bonus_board_waiters`），否则互相等成死锁。②**换盘后鸟回不了枝头**：预飞账本按格号记，牌被挪格时账本没跟着挪，`_complete_prelaunched_bird()` 就找不到那只鸟了；现在一并改写。③**换鸟弹窗**：原先只在货架下那条窄带里换标题、亮边框，决定感太弱；改成压暗背景 + 屏幕正中的卡片，写明「请谁住进来」、把窝里的鸟摆成大牌（最多 6 列，多了换行）、底下一颗「算了，不换」，弹窗期间整层吃掉点击，底下那条带子收起来免得重复。 | GameFlow, ShopEconomy | 奖励盘、鸟窝 | `scripts/main.gd`、`scripts/ui/shop_overlay.gd`、`tests/test_bonus_board.gd`、`tools/capture_shop_slots.gd`、QUICK_LOG |
| 27 | 2026-09-16 | 大盘超屏修复：棋盘锚在屏幕正中、读数叠（关卡名 / 剩余雷 / 分列道具）挂在它头顶，10×10 那档整块高 1155px 比 1080 的画布还高，「剩余雷」被顶出屏幕外看不见。新增 `_apply_solo_board_fit()`：把读数叠与棋盘当成一整块量画布，**先只往下挪，挪不开才把格子等比收一档**（8×8 只下移 28.5px；9×9 微收；10×10 格子 88→78.1 约 0.89、整块下移 74px），再摆进上下 12px 留白之间；只缩不放，塞得下的小盘一格不动。读数叠的行高/间距一并常量化（`BOARD_COUNTER_STACK_HEIGHT` 等），预留一律按最高一档算，免得有没有关卡名两种盘之间格子大小来回跳。顺手修了 `test_ui_layering` 里那条早于 `BOARD_VERTICAL_SHIFT` 的陈年断言。 | GameFlow | 剩余雷读数、分列道具读数 | `scripts/main.gd`、`tests/test_board_fit_scaling.gd`（新）、`tests/test_duel_split_layout.gd`、`tests/test_ui_layering.gd`、`tools/capture_big_board.gd`（新） |
| 27 | 2026-09-16 | 鸟窝**上限提到 15 格**，面板换成汇总样式。扩建改为一次 +3 格（仍是最多三次），于是 6 + 3×3 = 15；老档还原时补的扩建次数也跟着按 3 算。面板不再一格一张牌——15 格一格一张就挤没了，改成【总数 X / Y ｜ 鸟A×a 鸟B×b……】：左边写总数，中间一道分割块，右边按鸟归类，每种一张牌印「头像 + 名字×几只」（最多七种，排得很松）。换鸟弹窗同样按鸟归类，点一种就请走它当中的一只（同种鸟占哪一格都一样，落点取它在窝里的第一格，`slot_replace_chosen` 的契约不变）。 | ShopEconomy | 鸟窝 | `scripts/main.gd`、`scripts/ui/shop_overlay.gd`、`tests/test_endless_slot_shop.gd`、`tools/capture_shop_slots.gd`、QUICK_LOG、NAVIGATION |
| 15 | 2026-09-16 | 选关彩蛋**按关分家**：蚊子收进第 1 关（青草坡），第 2 关（麦垄）换成**地面上游荡的动物 + 薅毛**。四只动物（狗熊 / 猴子 / 企鹅 / 狐狸）用用户给的四张扁平插画抠的图（`tools/build_wander_animals.py`：四边按颜色距离泛洪去掉纯色背景、贴边裁切、统一高度）。**动物是在岛的格子坐标里走的**：位置是展品的 (c, r)，每帧按当前相机投影成屏幕坐标；走的格子从布局掩码里挑陆地格、只取岛的前半截，脚下取那一格的地形高度，所以上土丘也贴着地面。第一版用「屏幕空间矩形」当活动区，等距岛面在屏幕上是菱形，动物大半飘在岛外——用户当场指出「没走在关卡模型的地面上」，于是改成格子坐标。相机是正交的，远近不改变大小，缩放只按「一格多少像素」算。**鼠标停到动物身上**（不用点）乌鸦就俯冲下来薅一撮毛：复用乌鸦解锁页那四帧（探身 / 薅下 / 僵住 / 逃走）与毛絮贴图、惨叫音效，「薅狗熊毛！」弹在头顶，毛打着旋儿落下，动物撒腿跑下岛、乌鸦叼着毛飞走。薅够 3 种给成就【薅遍四野】。出现间隔 2.2～4.5 秒，开场 0.6～1.4 秒就来第一只。 | WorldMap, Achievements | 游荡动物（新）、薅遍四野（新成就） | `scripts/ui/map_wander_animal.gd`（新）、`scripts/ui/stage_cabinet.gd`（按关开彩蛋 `_sync_easter_eggs`、`wander_animal()` 探针）、`scripts/game/achievement_catalog.gd`、`tools/build_wander_animals.py`（新）、`my_asset/source/wander/`（新，用户原图）、`assets/sprites/generated/wander/`（新，抠好的四张）、`tools/capture_wander_animal.gd`（新）、`tests/test_stage_cabinet.gd` |
